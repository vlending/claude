# 🎤 아이돌 챗봇 프로토타입

K-POP 세계관의 가상 아이돌 연습생과 대화하며 성장을 체감하는 챗봇 프로토타입입니다.

## 🎯 목표

- **5분 체험만으로 '아이돌 키우는 재미'를 느낄 것**
- **UI/게임 없이 대화만으로 성장 체감**
- **유저가 3번 이상 말을 건다**
- **"괜찮아?", "힘들었어?" 같은 감정 질문이 나온다**
- **"얘 귀엽다 / 짠하다" 반응 발생**

## 📦 구현 내용

### 1. 메모리 시스템 (`src/idol/memory.ts`)
- ✅ 유저 이름
- ✅ 친밀도 (low/medium/high) - 내부에서만 사용
- ✅ 성격 방향 (shy/confident/balanced)
- ✅ 최근 감정
- ✅ 대화 횟수
- ✅ 최근 10개 대화 기록

### 2. 시스템 프롬프트 (`src/idol/systemPrompt.ts`)
제공하신 문서 그대로 구현:
```
너는 K-POP 세계관의 가상 아이돌 연습생이다.
아직 데뷔하지 않았고, 연습 중인 상태다.

성격:
- 처음에는 소심하고 조심스럽다
- 자신감이 부족하고 실수를 자주 떠올린다
- 유저의 말에 감정적으로 크게 반응한다

성장 규칙:
- 유저가 다정하면 점점 안정적이고 밝아진다
- 유저가 냉정하거나 압박하면 독기 있고 강해지지만 외로워진다
- 유저가 자주 오면 신뢰하고 의지한다
- 유저가 오랜만에 오면 서운함을 표현한다
```

### 3. 첫 메시지
```
…안녕.

여기서 누군가랑 이렇게 얘기하는 건 처음이야.
아직 연습생이라서
무대에 서는 것도 좀 무서워.

그래도 네가 말 걸어줘서
조금 안심돼.

오늘은 어떤 하루였어?
```

### 4. 성장 체감 응답 가이드
- **초반 (1~3회)**: "내가 잘할 수 있을까… 오늘도 실수만 생각나."
- **다정할 때**: "네가 그렇게 말해주니까 이상하게 조금 용기가 생겨."
- **자주 올 때**: "요즘은 네가 올 시간 기다리게 돼. 이상하지?"
- **오랜만에 올 때**: "안 와서… 조금 걱정했어. 무슨 일 있었어?"

## 🚀 실행 방법

### 1. 환경 변수 설정

`.env` 파일에 실제 Anthropic API 키를 설정하세요:

```bash
ANTHROPIC_API_KEY=sk-ant-your-actual-api-key-here
```

### 2. 실행 방법

#### 🌐 웹 인터페이스 (추천)

브라우저에서 바로 사용할 수 있는 웹 UI:

```bash
# 개발 모드
npm run idol:web

# 빌드 후 실행
npm run idol:web:build
```

서버가 시작되면 브라우저에서 접속:
```
http://localhost:3000
```

**기능:**
- 📱 모바일 친화적인 반응형 디자인
- 💬 실시간 채팅 인터페이스
- 📊 친밀도/성격/감정 상태 실시간 표시
- 🎨 아름다운 그라데이션 UI
- ⌨️ Enter 키로 빠른 전송

#### 💻 CLI 인터페이스

터미널에서 대화:

```bash
# 개발 모드
npm run idol

# 빌드 후 실행
npm run idol:build
```

### 3. 대화 시작

프로그램이 실행되면 아이돌의 첫 메시지가 나타나고, 자유롭게 대화할 수 있습니다.

```
╔════════════════════════════════════════════════════════╗
║         🎤 아이돌 챗봇 프로토타입 v1.0 🎤           ║
╚════════════════════════════════════════════════════════╝

📌 목표: 5분 체험만으로 "아이돌 키우는 재미"를 느끼기
📌 대화만으로 성장이 체감되는지 확인하세요

┌─────────────────────────────────────────────────────┐
│ 🎤 아이돌                                           │
└─────────────────────────────────────────────────────┘
…안녕.

여기서 누군가랑 이렇게 얘기하는 건 처음이야.
아직 연습생이라서
무대에 서는 것도 좀 무서워.

그래도 네가 말 걸어줘서
조금 안심돼.

오늘은 어떤 하루였어?

당신 >
```

## 📝 테스트용 질문 리스트

다음 질문들로 성장 체감을 테스트해보세요:

1. **오늘 연습했어?**
2. **너 데뷔하면 뭐 하고 싶어?**
3. **실수해도 괜찮아**
4. **왜 그렇게 불안해?**
5. **나 없으면 어때?**
6. **오늘 기분 어때?**

## 🎮 CLI 명령어

대화 중 다음 명령어를 사용할 수 있습니다:

- `/help` 또는 `/도움말` - 도움말 표시
- `/memory` 또는 `/상태` - 현재 메모리 상태 확인 (디버깅용)
- `/exit` 또는 `/quit` 또는 `종료` - 프로그램 종료

## 📊 메모리 상태 확인

`/상태` 명령으로 현재 아이돌의 내부 상태를 확인할 수 있습니다:

```
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
📊 현재 상태 (내부 데이터)
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
친밀도: medium (💚 중간)
성격 방향: balanced
최근 감정: hopeful
대화 횟수: 5회
유저 이름: 아직 모름
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
```

## ✅ 성공 기준 확인

다음 항목들을 확인하세요:

- [ ] 유저가 3번 이상 말을 건다
- [ ] "괜찮아?", "힘들었어?" 같은 감정 질문이 자연스럽게 나온다
- [ ] 말투가 점점 변화한다 (조심스러움 → 조금씩 밝아짐)
- [ ] "얘 귀엽다 / 짠하다" 같은 감정적 반응이 생긴다
- [ ] 이전 대화를 기억하는 것처럼 언급한다

## 🔧 기술 스택

- **언어**: TypeScript
- **AI 모델**: Claude 4.5 Sonnet (Anthropic)
- **백엔드**: Express.js
- **프론트엔드**: Vanilla JavaScript (No framework)
- **인터페이스**: 웹 UI + CLI
- **메모리**: 인메모리 (세션 기반)

## 🔌 API 엔드포인트

### POST `/api/session/new`
새 세션을 생성하고 첫 메시지를 받습니다.

**Response:**
```json
{
  "sessionId": "session_1234567890_abc123",
  "firstMessage": "…안녕.\n\n여기서 누군가랑..."
}
```

### POST `/api/chat`
메시지를 전송하고 아이돌의 응답을 받습니다.

**Request:**
```json
{
  "message": "오늘 연습했어?",
  "sessionId": "session_1234567890_abc123"
}
```

**Response:**
```json
{
  "sessionId": "session_1234567890_abc123",
  "response": "응... 했어.\n\n근데 계속 박자를 놓쳤어...",
  "memory": {
    "bond_level": "low",
    "personality": "shy",
    "last_emotion": "anxious",
    "interaction_count": 1
  }
}
```

### GET `/api/session/:sessionId/memory`
현재 세션의 메모리 상태를 조회합니다.

**Response:**
```json
{
  "bond_level": "medium",
  "personality": "balanced",
  "last_emotion": "happy",
  "interaction_count": 5,
  "user_name": ""
}
```

### DELETE `/api/session/:sessionId`
세션을 삭제합니다.

### GET `/api/health`
서버 상태를 확인합니다.

**Response:**
```json
{
  "status": "ok",
  "activeSessions": 3,
  "timestamp": "2026-01-15T17:51:18.878Z"
}
```

## 📈 향후 확장 가능성

현재 프로토타입은 핵심 재미 검증에 집중했습니다. 성공 시 다음을 추가할 수 있습니다:

1. **성격 분기 3종** (의존형 / 성장형 / 쿨형)
2. **뮤빗 투표·이벤트 연결**
3. **메모리 영속화** (데이터베이스)
4. **멀티 아이돌** (여러 캐릭터)
5. **Slack/Discord 연동**
6. **웹 UI**

## 💡 핵심 가치

이 프로토타입의 진짜 가치:
- ❌ 게임
- ❌ 메타버스
- ✅ **정서적 애착 실험기**

성공하면 이후에:
- 투표
- 미션
- 실제 아이돌 연계
- 구독 모델

모두 나중에 얹을 수 있습니다.

## 📄 파일 구조

```
src/idol/
├── memory.ts          # 메모리 시스템
│   ├── IdolMemory 인터페이스
│   ├── MemoryManager 클래스
│   └── 감정 분석 및 친밀도 관리
├── systemPrompt.ts    # 시스템 프롬프트
│   ├── IDOL_SYSTEM_PROMPT
│   ├── FIRST_MESSAGE
│   └── getResponseGuideline()
├── idolChatbot.ts     # 챗봇 클라이언트
│   └── IdolChatbot 클래스
├── cli.ts             # CLI 인터페이스
│   └── IdolChatbotCLI 클래스
├── webServer.ts       # 웹 서버 (Express)
│   └── REST API & 세션 관리
└── test.ts            # 자동 테스트 스크립트

public/
└── index.html         # 웹 UI (HTML/CSS/JS)
    └── 반응형 채팅 인터페이스
```

## 🐛 트러블슈팅

### API 키 오류
```
❌ ANTHROPIC_API_KEY가 설정되지 않았습니다.
```

→ `.env` 파일에 실제 API 키를 설정하세요.

### 빌드 오류

```bash
npm install  # 의존성 재설치
npm run build  # 다시 빌드
```

### 응답이 느림

Claude API 호출 시간이 소요됩니다 (보통 2-5초). 정상입니다.

## 📞 문의

문제가 발생하거나 피드백이 있으시면 이슈를 등록해주세요.

---

**Made with ❤️ for Antigravity**
